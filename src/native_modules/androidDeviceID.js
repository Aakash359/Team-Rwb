'use strict';

/**
 * This exposes the native DeviceID module as a JS module. This has a
 * function 'show' which takes the following parameters:
 *
 */

import {NativeModules} from 'react-native';
module.exports = NativeModules.DeviceID;
