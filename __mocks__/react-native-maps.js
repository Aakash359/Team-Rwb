import { createComponent } from './utils/createComponent';

const MapView = createComponent('MapView');
const PROVIDER_GOOGLE = createComponent('PROVIDER_GOOGLE');

export {
    MapView,
    PROVIDER_GOOGLE
};

export default MapView;